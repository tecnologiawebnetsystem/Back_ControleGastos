#!/usr/bin/env bash
# Arquivo para configurações durante o build no Render

# Garantir que o diretório de logs exista
mkdir -p logs

# Configurar variáveis de ambiente específicas do Render
echo "Configurando variáveis de ambiente para o Render..."

